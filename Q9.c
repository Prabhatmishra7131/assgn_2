#include<stdio.h>
int main()
{
    int a;
    char ch;
    float b;
    printf("Size of the integer is: %d\n",sizeof(a));
    printf("Size of the character is: %d\n",sizeof(ch));
    printf("Size of the float is: %d\n",sizeof(b));
    return 0;
}